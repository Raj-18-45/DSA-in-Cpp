#include <iostream>
#include <vector>
using namespace std;

int largestElement(vector<int> nums){
    int n = nums.size();
    int largest = nums[0];
    
    for(int i = 0; i < n; i++){
        if(nums[i] > largest){
            largest=nums[i];
        }
    }
    return largest;
}

int main(){
    vector<int> v = {1,2,3,4,5,6};
    v.push_back(7);

    int store= largestElement(v);
    cout<< "largest element:" <<store;

    
}

//  Time Complexity:
// 	
// 	•	Worst: O(n)

//  Space Complexity:
// 	•	Current code: O(n)
// 	•	Optimized code: O(1)
